<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="4dp">

    <TextView
        android:id="@+id/tvAiMessage"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_ai_bubble"
        android:paddingStart="16dp"
        android:paddingTop="12dp"
        android:paddingEnd="16dp"
        android:paddingBottom="12dp"
        android:text="Hello! How can I help you today?"
        android:textColor="@color/ai_text"
        android:textSize="16sp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintWidth_max="300dp" />

    <TextView
        android:id="@+id/tvAiTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        android:text="10:01 AM"
        android:textColor="#757575"
        android:textSize="10sp"
        app:layout_constraintStart_toStartOf="@+id/tvAiMessage"
        app:layout_constraintTop_toBottomOf="@+id/tvAiMessage" />

    <ImageButton
        android:id="@+id/btnCopy"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginStart="8dp"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:src="@drawable/ic_copy"
        app:tint="#757575"
        app:layout_constraintBottom_toBottomOf="@+id/tvAiTimestamp"
        app:layout_constraintStart_toEndOf="@+id/tvAiTimestamp"
        app:layout_constraintTop_toTopOf="@+id/tvAiTimestamp" />

</androidx.constraintlayout.widget.ConstraintLayout>
